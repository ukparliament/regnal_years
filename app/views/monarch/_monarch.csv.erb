<%= monarch.title %>,<%= monarch.date_of_birth.strftime( '%Y-%m-%d' ) %>,<%= monarch.date_of_death.strftime( '%Y-%m-%d' ) if monarch.date_of_death %>,<%= monarch.wikidata_id if monarch.wikidata_id %>
